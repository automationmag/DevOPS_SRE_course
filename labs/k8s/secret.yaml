# Аналог секретов .env. В учебнике — пустые/фейковые.
# В живом кластере не коммитят настоящие значения; кладут из CI или менеджера секретов.
apiVersion: v1
kind: Secret
metadata:
  name: pulse-secret
  namespace: pulse
type: Opaque
stringData:
  EXAMPLE_TOKEN: "not-a-real-token"
